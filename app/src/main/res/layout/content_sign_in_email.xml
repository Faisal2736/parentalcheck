<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context=".uiActivities.ChildSignInEmailActivity"
    tools:showIn="@layout/activity_sign_in_email"
    android:background="#7c60c6"
    android:padding="30dp">
    <android.support.design.widget.TextInputLayout
        android:id="@+id/nameTextInput"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        app:errorTextAppearance="@style/MyErrorText">

        <EditText
            android:id="@+id/name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:inputType="text"
            android:paddingLeft="5dp"
            android:drawablePadding="10dp"
            android:theme="@style/MyErrorText"
            android:hint="Parent Email Address"/>

    </android.support.design.widget.TextInputLayout>

    <Button
        android:id="@+id/btn_next_signIn"
        android:layout_width="match_parent"
        android:layout_height="@dimen/btn_height"
        android:background="@color/md_white_1000"
        android:text="Next"
        android:textStyle="bold"
        android:layout_alignParentBottom="true"
        android:textColor="#7c60c6" />
</RelativeLayout>